# 新三国翻译器 · 真实部署指南（服务端，可真翻译）

> 之前 CloudStudio 部署的是**静态 demo 版**（无服务端，只能演示、不调模型）。
> 要公开链接也能**真翻译**，必须部署到带 Node 运行时的服务端平台，并配置 3 个环境变量。
> 代码已就绪：`next.config.ts` 无 `output:"export"`、`app/api/xinsanguo/route.ts` 存在、`.env.local` 三 key 齐全。

---

## 方案 A：Vercel 一键导入（推荐，最快）

仓库已公开：`github.com/Yuexiye/zhouli-translator`

1. 打开 https://vercel.com/new → 导入该 GitHub 仓库
2. Framework 自动识别 Next.js（也已有 `vercel.json` 兜底）
3. **配置环境变量**（Settings → Environment Variables，Production 也勾上）：
   | 变量名 | 说明 |
   |---|---|
   | `OPENAI_COMPATIBLE_BASE_URL` | 含 `/v1` 的网关地址，如 `https://api.hcnsec.cn/v1` |
   | `OPENAI_COMPATIBLE_API_KEY` | 你的网关 API Key |
   | `OPENAI_COMPATIBLE_MODEL` | 模型名，如 `DeepSeek-V4-Flash` |
4. 点 **Deploy**，约 1–2 分钟完成
5. 拿到 `*.vercel.app` 域名，`/xinsanguo` 即真实翻译

> 本地 `.env.local` 的值直接搬过去即可（不要提交 `.env.local` 到仓库）。

---

## 方案 B：CloudStudio 应用 / 其他 Node 平台

- CloudStudio「应用」模式（非静态）可跑 `next start`；需自建启动命令并配同样 3 个环境变量。
- Railway / Render 同理：连 GitHub 仓库 → 设 3 个 env → 自动识别 Next.js 部署。

---

## 部署后验证清单
- [ ] 首页 `/` 正常
- [ ] `/xinsanguo` 输入"今天天气真好啊"能返回"不可能！绝对不可能！"风格文案（非 demo 占位）
- [ ] 切换模型/Provider 生效
- [ ] 手机端可访问
- [ ] 限流（12次/10分钟）生效（防止被刷）

## 注意事项
- 当前 `route.ts` 限流 12 次/10 分钟，公开部署偏紧，可在 `app/api/xinsanguo/route.ts` 调整。
- `next.config.ts` 切勿加 `output:"export"`，否则 API 路由被剥离、无法真翻译。
- 模型调用走你自己的网关配额，注意费用。
